import 'package:flutter/material.dart';

class HistoryArticles extends StatelessWidget {
  const HistoryArticles({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: const Text('閲覧履歴'),
          backgroundColor: Colors.blue,
        ),
      body: const Text('Coming Soon!!'),
    );
  }
}
